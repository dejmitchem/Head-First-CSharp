<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoreWeaponsDamageCalculator.MainPage">

    <ScrollView>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Text="Flaming" FontSize="Medium" VerticalOptions="Center" HorizontalOptions="End"/>
            <CheckBox x:Name="Flaming" Grid.Column = "1" VerticalOptions="Center" HorizontalOptions="Start"
                      Margin="20,0,0,0" CheckedChanged="Flaming_CheckedChanged"/>

            <Label Grid.Column="2" Text="Magic" FontSize="Medium" VerticalOptions="Center" HorizontalOptions="End"/>
            <CheckBox x:Name="Magic" Grid.Column = "3" VerticalOptions="Center" HorizontalOptions="Start"
                      Margin="20,0,0,0" CheckedChanged="Magic_CheckedChanged"/>

            <Button x:Name="Roll" Grid.Row="1" Grid.ColumnSpan="4" Text="Roll for damage" BackgroundColor="DarkGray" BorderColor="Black"
                    BorderWidth="2" FontSize="Medium" Clicked="Button_Clicked"/>

            <Label x:Name="Damage" Grid.Row="2" Grid.ColumnSpan="4" Margin="0,20,0,0" BackgroundColor="Black" TextColor="White"
                   FontSize="Large" VerticalTextAlignment="Center" HorizontalTextAlignment="Center"/>

        </Grid>
    </ScrollView>

</ContentPage>
